# Shared

This is the part of the setup I keep boring on purpose: the stuff that should behave the same
on Arch and macOS, no matter how fancy the rest of the machine gets.

It covers the common baseline I actually want everywhere (e.g. shells, terminals, editors,
tmux, git, etc.), without duplicating myself or letting each platform drift off into its
own little mess.
